DEBUG="true"
# Session cookie Secure flag (HTTPS-only). Leave unset: uses not DEBUG (false while DEBUG=true).
# Set to false on HTTPS-terminated reverse proxies if cookies fail. Set true to force Secure.
# DJANGO_AUTH_COOKIE_SECURE="false"
PORT="4000"
# Test email: POST /api/test-email + header X-Email-Test-Secret
EMAIL_TEST_SECRET="change-me-to-a-long-random-string"
DATABASE_URL="postgres://USER:PASSWORD@HOST:5432/postgres?sslmode=require"
JWT_SECRET="replace-with-a-strong-secret"
SUPER_ADMIN_EMAIL="superadmin@attendance.local"
SUPER_ADMIN_PASSWORD="Admin@12345"
SUPER_ADMIN_NAME="Super Admin"
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="attendance@system.local"
